name: Keep Alive

on:
  schedule:
    - cron: "17 4 1 * *"  # 1st of month at 04:17 UTC — off-peak, off-:00 mark
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "keep-alive ping at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
